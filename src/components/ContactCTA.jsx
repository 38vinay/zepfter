import React from "react";

const ContactCTA = () => {
  return (
    <section className="section text-center bg-primary text-light">
      <h2 className="fw-bold">Ready to Start Your Career?</h2>
      <p className="mt-2">Contact us today to enroll in our training programs.</p>
      <a href="/contact" className="btn btn-light btn-lg mt-3">Contact Us</a>
    </section>
  );
};

export default ContactCTA;
